'use strict';
import routes from './route';

const menu = {
    name: 'menu',
    version: '1.0.0',
    register: async function (server, options) {
        //server.route(routes);
    }
};

module.exports = menu;
