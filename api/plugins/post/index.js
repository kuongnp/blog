'use strict';
import routes from './route';

const post = {
    name: 'post',
    version: '1.0.0',
    register: async function (server, options) {
        //server.route(routes);
    }
};

module.exports = post;