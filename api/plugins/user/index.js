'use strict';
import routes from './route';

const user = {
    name: 'user',
    version: '1.0.0',
    register: async function (server, options) {
        //server.route(routes);
    }
};

module.exports = user;
