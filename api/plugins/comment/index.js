'use strict';
import routes from './route';

const comment = {
    name: 'comment',
    version: '1.0.0',
    register: async function (server, options) {
        //server.route(routes);
    }
};

module.exports = comment;